name: Coverage Frontend
on:
  push:
    branches: [ main ]
    paths: [ 'frontend/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'frontend/**' ]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: coverage-front-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: ./frontend/package-lock.json

      - name: Frontend Tests
        working-directory: ./frontend
        run: |
          npm install --legacy-peer-deps
          npm install -D @vitest/coverage-v8
          npm exec vitest -- run --coverage --coverage.reporter=json-summary --coverage.reporter=text
        env:
          CI: true

      - name: Generate Frontend Badges
        uses: jpb06/coverage-badges-action@latest
        with:
          coverage-summary-path: ./frontend/coverage/coverage-summary.json
          badges-icon: vitest
          output-folder: ./.github/badges/frontend
          commit-message: "docs: update frontend coverage badges [skip ci]"